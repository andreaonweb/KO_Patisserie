services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ko
      POSTGRES_PASSWORD: ko
      POSTGRES_DB: ko_patisserie
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
